import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    photoMed: {
        width: 120,
        height: 120,
        marginLeft: 8,
        marginRight: 8
    },
    textMed: {
        marginLeft: 8
    }
})

export default styles